import "./NavBar.css"

const NavBar = (props) => {
    return (
        <nav>
            <h2 className="header">
                <p className="header-text">InnerVoice</p>
            </h2>
        </nav>
    );
}

export default NavBar